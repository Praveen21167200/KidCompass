# Server config. Copy to .env and adjust, or set as environment variables.
PORT=3000
# Secret used to sign JWTs. CHANGE THIS in production.
JWT_SECRET=change-me-in-production
# Google OAuth *Web* client ID (same one used by the Android app).
# Required to verify Google ID tokens sent by the app.
GOOGLE_WEB_CLIENT_ID=
